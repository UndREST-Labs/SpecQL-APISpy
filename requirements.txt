# SpeQL - API Spec Query Analyser
# Currently supporting Azure REST API
# Designed to identify SilentReaper vulnerabilities
# Python dependencies for the CLI menu system

# For rendering the SpeQL logo in ASCII art
pyfiglet>=0.8.0

# For portal_sweep.py — Azure Portal APISpy sweep
azure-identity>=1.15.0  # DeviceCodeCredential for Azure CLI first-party app auth
playwright>=1.40.0      # Browser automation (run: python -m playwright install chromium)
